<Application x:Class="WpfApp1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <!-- NO StartupUri — MainWindow launched from App.xaml.cs after theme loads -->
    <Application.Resources>

        <!-- ── Fallback Colors ── -->
        <SolidColorBrush x:Key="PlayerBackgroundBrush"   Color="#111111"/>
        <SolidColorBrush x:Key="PlayerSurfaceBrush"      Color="#1E1E1E"/>
        <SolidColorBrush x:Key="PlayerElevatedBrush"     Color="#2D2D30"/>
        <SolidColorBrush x:Key="PlayerHoverBrush"        Color="#3A3A3E"/>
        <SolidColorBrush x:Key="PlayerBorderBrush"       Color="#3F3F46"/>
        <SolidColorBrush x:Key="PlayerForegroundBrush"   Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PlayerTextPrimaryBrush"  Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PlayerTextSecondaryBrush" Color="#AAAAAA"/>
        <SolidColorBrush x:Key="PlayerMutedBrush"        Color="#64748B"/>
        <SolidColorBrush x:Key="PlayerPrimaryBrush"      Color="#007ACC"/>
        <SolidColorBrush x:Key="PlayerPrimaryHoverBrush" Color="#005A9E"/>
        <SolidColorBrush x:Key="PlayerSecondaryBrush"    Color="#14B8A6"/>
        <SolidColorBrush x:Key="PlayerAccentBrush"       Color="#F97316"/>
        <SolidColorBrush x:Key="PlayerDangerBrush"       Color="#EF4444"/>
        <SolidColorBrush x:Key="PlayerFocusRingBrush"    Color="#60A5FA"/>
        <SolidColorBrush x:Key="PlayerControlBarBrush"   Color="#CC222222"/>
        <!-- Button -->
        <SolidColorBrush x:Key="PlayerButtonBgBrush"     Color="#3B82F6"/>
        <SolidColorBrush x:Key="PlayerButtonFgBrush"     Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PlayerButtonBorderBrush" Color="#326DCE"/>
        <SolidColorBrush x:Key="PlayerButtonHoverBrush"  Color="#2563EB"/>
        <SolidColorBrush x:Key="PlayerButtonFocusBrush"  Color="#60A5FA"/>
        <!-- Card -->
        <SolidColorBrush x:Key="PlayerCardBgBrush"       Color="#1E1E1E"/>
        <SolidColorBrush x:Key="PlayerCardFgBrush"       Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PlayerCardBorderBrush"   Color="#3F3F46"/>
        <SolidColorBrush x:Key="PlayerCardHoverBrush"    Color="#2D2D30"/>

        <!-- ── Fallback Fonts ── -->
        <FontFamily x:Key="PlayerFontFamily">Inter</FontFamily>
        <FontFamily x:Key="PlayerFontFamilyUi">Inter</FontFamily>
        <FontFamily x:Key="PlayerFontFamilyBody">Inter</FontFamily>
        <FontFamily x:Key="PlayerFontFamilyHeading">Inter</FontFamily>

        <!-- ── Fallback Font Sizes ── -->
        <sys:Double x:Key="PlayerFontSizeXs">11</sys:Double>
        <sys:Double x:Key="PlayerFontSizeSm">13</sys:Double>
        <sys:Double x:Key="PlayerFontSizeBase">16</sys:Double>
        <sys:Double x:Key="PlayerFontSizeLg">19</sys:Double>
        <sys:Double x:Key="PlayerFontSizeXl">23</sys:Double>
        <sys:Double x:Key="PlayerFontSize2Xl">27</sys:Double>
        <sys:Double x:Key="PlayerButtonFontSize">16</sys:Double>

        <!-- ── Fallback Corner Radii ── -->
        <CornerRadius x:Key="PlayerRadiusSm">4</CornerRadius>
        <CornerRadius x:Key="PlayerRadiusMd">8</CornerRadius>
        <CornerRadius x:Key="PlayerRadiusLg">12</CornerRadius>
        <CornerRadius x:Key="PlayerRadiusXl">16</CornerRadius>
        <CornerRadius x:Key="PlayerButtonRadius">8</CornerRadius>
        <CornerRadius x:Key="PlayerCardRadius">12</CornerRadius>
        <CornerRadius x:Key="PlayerInputRadius">4</CornerRadius>
        <CornerRadius x:Key="PlayerModalRadius">16</CornerRadius>

        <!-- ── Fallback Font Weights ── -->
        <FontWeight x:Key="PlayerButtonFontWeight">Medium</FontWeight>

    </Application.Resources>
</Application>
